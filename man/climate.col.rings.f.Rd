% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/climate.stripes.r
\name{climate.col.rings.f}
\alias{climate.col.rings.f}
\title{Draws climate rings given a time vector and environmental (usually temperature) vector}
\usage{
climate.col.rings.f(
  time.vector,
  temperature.vector,
  colour.vec = c("navyblue", "lightblue", "red", "darkred"),
  stripe.cols = NULL,
  title = "",
  legend = TRUE,
  text.col.legend = "yellow",
  na.colour = "white",
  nv = 1000,
  legend.pos = c(6, -4, 8.5, 4),
  ...
)
}
\arguments{
\item{time.vector}{the time series vector}

\item{temperature.vector}{a time series of any environmental variable whose values correspond
to the time vector}

\item{colour.vec}{a vector of colours to ramp between (see colorRampPalette). Also used for
the legend colour gradient regardless of whether \code{stripe.cols} is supplied}

\item{stripe.cols}{a vector of pre-computed colours, one per time step, as returned invisibly
by \code{climate.col.stripes.f}. If supplied these are used directly for ring colours so
that the rings and stripes share an identical colour mapping}

\item{title}{a title for the plot. Default ""}

\item{legend}{puts a gradient legend on the plot. Default TRUE}

\item{text.col.legend}{colour of the legend text. Default "yellow"}

\item{na.colour}{colour to display for NA values in the time series. Default "white"}

\item{nv}{number of vertices used to draw each circle (higher = smoother). Default 1000}

\item{legend.pos}{a length-4 vector giving c(xleft, ybottom, xright, ytop) for the colour
gradient legend in plot coordinates. Default c(6.0, -4, 8.5, 4)}

\item{...}{additional arguments passed to \code{plot}}
}
\description{
Climate rings are a circular variant of climate stripes. The most recent year is
  the outermost ring and the oldest year is at the centre, so the progression from cool (old,
  inner) to warm (recent, outer) reads naturally from inside to outside, like a warming tree.
}
\details{
Rings are drawn from the most recent year (largest radius, outermost) inward to the
  oldest year (smallest radius, innermost). Each ring is a filled circle so it covers the
  centre of everything drawn before it. Drawing from outside in means the oldest year ends up
  as the visible centre dot. Radii are evenly spaced by index.
}
\references{
https://www.climate-lab-book.ac.uk/2018/warming-stripes/
}
\seealso{
climate.col.stripes.f plotrix::draw.circle
}
